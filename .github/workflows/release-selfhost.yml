# release-selfhost — cut a versioned self-host release.
#
# Self-host operators should pin to a RELEASE tag (e.g. v0.1.0), not :latest or a
# raw sha. This workflow retags the already-built+tested self-host images (the
# per-commit `sha-<sha>` / `latest` builds) to a version tag — no rebuild, so the
# released artifacts are byte-identical to what was tested.
#
# Images are amd64-only: hosted prod (DOKS), the homelab gamma canary, and the CI
# runners are all amd64, so continuous builds are amd64-only and self-host targets
# amd64. (To re-add ARM self-host: build the continuous images multi-arch again, or
# restore the arm64-rebuild-at-release step from git history — commit 9c86e5f.)
#
# Usage: Actions → release-selfhost → Run workflow → version=v0.1.0
#   (optionally source_tag=<sha-... or latest> to pick the exact build to release).
name: release-selfhost

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Release version tag to publish (e.g. v0.1.0)"
        required: true
      source_tag:
        description: "Existing image tag to promote (default: latest)"
        required: false
        default: latest

permissions:
  packages: write

jobs:
  tag:
    runs-on: homelab-agentkit-linux-docker-build
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Promote self-host images to ${{ inputs.version }}
        run: |
          set -euo pipefail
          IMAGES="agentkitforge agentkitmarket-core agentkitmarket-app agentkitauto-app agentkitauto-worker agentkitprofile-app"
          for img in $IMAGES; do
            src="ghcr.io/agentkitproject/${img}:${{ inputs.source_tag }}"
            dst="ghcr.io/agentkitproject/${img}:${{ inputs.version }}"
            echo "::group::${img}: ${{ inputs.source_tag }} -> ${{ inputs.version }}"
            docker buildx imagetools create --tag "$dst" "$src"
            docker buildx imagetools inspect "$dst" --format '{{.Manifest.MediaType}}' | head -1
            echo "::endgroup::"
          done
          echo "Released self-host images at ${{ inputs.version }}: $IMAGES"
