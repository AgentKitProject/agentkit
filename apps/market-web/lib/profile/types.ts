export type PublicPublisherProfile = {
  displayName: string | null;
  handle: string | null;
  avatarInitials: string | null;
  verified: boolean;
};

export type PublisherSnapshot = PublicPublisherProfile;
